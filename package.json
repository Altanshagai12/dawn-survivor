{
  "name": "dawn-survivor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.js",
    "repair:sprites": "node scripts/repair-sprite-atlases.mjs --write",
    "check": "node scripts/repair-sprite-atlases.mjs --check && node scripts/check-assets.mjs && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
